apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: micro-demo
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: micro-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - name: http
      port: 3000
      targetPort: 3000
