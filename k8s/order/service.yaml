apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: micro-demo
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/part-of: micro-demo
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
