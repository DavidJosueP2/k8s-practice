apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: micro-demo
  labels:
    app.kubernetes.io/name: prometheus
    app.kubernetes.io/part-of: micro-demo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prometheus
  ports:
    - name: http
      port: 9090
      targetPort: 9090
